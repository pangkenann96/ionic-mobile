import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-playground',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
